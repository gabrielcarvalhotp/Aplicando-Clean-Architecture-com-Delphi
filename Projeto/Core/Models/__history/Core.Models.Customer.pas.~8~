unit Core.Models.Customer;

interface

type
  TCustomer = class
  private
    FId: Integer;
    FName: string;
    FDocument: string;
    FAddress: string;
    FPhone: string;
  public
    property Id: Integer read FId write FId;
    property Name: string read FName write FName;
    property Document: string read FDocument write FDocument;
    property Address: string read FAddress write FAddress;
    property Phone: string read FPhone write FPhone;

    procedure RulesBusinessValidation;
  end;

implementation

uses
  System.SysUtils, Core.Exceptions.CustomerExceptions;

{ TCustomer }

procedure TCustomer.RulesBusinessValidation;
begin
  if Trim(FName) = '' then
    raise CustomerValidationException.Create('Nome deve ser informado');

  if Length(FName) < 4 then
    raise CustomerValidationException.Create('Nome deve conter no minímo 4 caracteres');

  if Trim(FDocument) = '' then
    raise CustomerValidationException.Create('Documento deve ser informado');

  if Length(FDocument) <= 4 then
    raise CustomerValidationException.Create('Documento deve conter no minímo 4 caracteres');

  if Trim(FPhone) = '' then
    raise CustomerValidationException.Create('Telefone deve ser informado');

  if Length(FPhone) <= 4 then
    raise CustomerValidationException.Create('Documento deve conter no minímo 4 caracteres');;
end;

end.
